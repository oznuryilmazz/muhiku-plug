jQuery(function (e) {
  e("#log-viewer-select").on("click", "h2 a.page-title-action", function (e) {
    return (
      e.stopImmediatePropagation(),
      window.confirm(muhiku_forms_admin_tools.delete_log_confirmation)
    );
  });
});
